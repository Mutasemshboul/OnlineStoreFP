@model IEnumerable<OnlineStoreFP.Models.VisacarFp>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Numbercard)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Balance)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.User)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Numbercard)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Balance)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.User.Userid)
            </td>
            <td>
                <a class="fa fa-edit" asp-action="Edit" asp-route-id="@item.Visaid"> </a> |
                <a asp-action="Details" asp-route-id="@item.Visaid">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Visaid">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
